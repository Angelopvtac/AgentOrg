Evaluate a project for public release readiness: $ARGUMENTS

If no project path given, ask which project under `development/projects/` to evaluate.

Delegate to the `project-eval` skill with the resolved project path.
